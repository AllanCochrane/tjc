<%= form_for(lesson_step) do |f| %>
  <% if lesson_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_step.errors.count, "error") %> prohibited this lesson_step from being saved:</h2>

      <ul>
      <% lesson_step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lesson_id %>
    <%= f.text_field :lesson_id %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_field :text %>
  </div>

  <div class="field">
    <%= f.label :media_type %>
    <%= f.text_field :media_type %>
  </div>

  <div class="field">
    <%= f.label :file_path %>
    <%= f.text_field :file_path %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
